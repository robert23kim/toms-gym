# Project Preferences

## Deployment
- Always deploy changes and test in production after completing frontend/backend modifications
- Use `python deploy.py --frontend-only` for frontend-only changes
- Use `python deploy.py --backend-only` for backend-only changes
- Use `python deploy.py` for full deployment

## Production URLs
- Frontend: https://my-frontend-quyiiugyoq-ue.a.run.app
- Backend: https://my-python-backend-quyiiugyoq-ue.a.run.app
