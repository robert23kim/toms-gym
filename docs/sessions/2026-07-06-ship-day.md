# Session Log — 2026-07-06 "Ship Day"

One Claude Code session (Opus 4.8), 37 commits (`f21dab7..05c74ad`), five features designed, built TDD-style, deployed to production, click-through verified on real data, and pushed. Frontend suite grew 36 → **50 suites / 312 tests**; backend CI gate grew to **138 tests**.

## Features shipped (in order)

### 1. Easy Upload Flows
Spec/plan: `docs/superpowers/{specs,plans}/2026-07-06-easy-upload-flows*`. Commits `67a9056..1586e58`.
- Camera-first `capture="environment"` inputs on golf scorecard upload and challenge video upload ("Record now")
- `/golf/snap` auto-camera route + PWA manifest shortcut; GolfHub CTA repointed
- `HandicapResultCard`: confirmed-round screen leads with the handicap index + signed delta vs the pre-confirm snapshot
- LiftHub dynamic "Plank challenge" quick link (ongoing + `Plank` category)
- **Still open:** real-device confirmation that `capture` opens the phone camera (not verifiable in emulators)

### 2. Home Redesign + Quiet-Gym Cohesion
Spec/plan: `…/2026-07-06-home-redesign-cohesion*` (+ committed HTML mockup asset, iterated live in an Artifact with Tom). Commits `4a3b144..623168a`.
- Shared primitives: `AmbientBackground` (doodle wallpaper + drifting glows, mounted once in `Layout` → app-wide), `IconTile`, `RowCard`, `DemoLoop` (12s plank → bowl → golf CSS/SVG loop)
- Index rebuilt minimal + centered: hero → demo loop → 3 tiles → live open-challenges strip; **Top Lifts removed**, How-It-Works and photo cards removed
- Footer everywhere: "Report a bug · Request a feature · Terms · Privacy · v…" → `/feedback`
- HubPage/UploadChooser/Challenges/Leaderboard restyled to the same language; golf `fw-*` pages untouched

### 3. Plank Stats v1 (steadiness pack + personality)
Spec/plan: `…/2026-07-06-plank-stats-v1*`. Commits `5bc3167..077e76e`. **Frontend-only** — the engine had emitted `per_second[]` since day one; the UI never rendered it (verified on prod report before building — no re-analysis needed).
- `lib/plankStats.ts`: holdRuns / steadinessScore / wobbleEvents / decayPoint / milestones / personality (Jelly 🪼 → Phoenix 🔥 → Slow Melter 🫠 → Statue 🗿 → Steady Eddie 💪), 18 threshold-pinned tests
- `PlankSteadiness`: scrubbable SVG timeline (tap → video seeks), state bands, wobble diamonds, milestone ticks, decay annotation, hold-segments strip
- Prod verify on the real 244s plank: **79 Steady · 🔥 Phoenix · "form held 89s"**

### 4. Lift History on Profile
Spec/plan: `…/2026-07-06-lift-history*`. Commits `4bb0232..936faa0`.
- Root cause: profile fed a `LIMIT 10` thumbnail wall with zero analysis data
- `GET /users/<id>/lifts?limit&offset` upgraded in place (old version had no consumers): paginated, newest-first, grade/hold pulled from the JSONB report **in SQL** so `per_second` never ships on list rows; row shaping DB-free in `services/lift_history.py`
- `LiftHistoryList` on the profile Lift tab: date · type · weight · payoff pill, row → VideoPlayer, "Load more"
- Prod verify: wonder725 → `Jul 6 Plank 4:04 / Jul 5 3:35 / Jun 6 2:31`

### 5. Challenge Attempt History
Spec/plan: `…/2026-07-06-challenge-attempt-history*`. Commits `b4b18a5..05c74ad`.
- `competition_id` filter on the lifts endpoint (fixed-literal clause, no injection surface)
- `AttemptHistory` lazy panel; `N attempts ▾` chips on `LeaderboardRow`/`YouRow`; single-open accordion in `ChallengeDetail`
- **Mid-flight discovery:** the multi-attempt users are usually the top 3, and the podium isn't a LeaderboardRow — added a chips strip *below* the podium (cards stay pure visuals)
- Prod verify: expanded wonder725 → 4:04 🏆 / 3:35 / 2:31, links per attempt

## Deployment state at session end

- `my-frontend-00157-5rx` and `my-python-backend-00171-4ps`, both latest-ready at 100% traffic, health-checked
- `main` == `origin/main` (all pushed)

## Gotchas worth remembering

- **jest + this repo:** page tests must mock `../../config` (Vite `import.meta`) and stub `Layout`/`FairwayScope`/`Navbar`; jsdom lacks `PointerEvent` — dispatch `new MouseEvent("pointerdown", {clientX})` to reach `onPointerDown` with coordinates
- **VideoPlayer route** is `/challenges/:id/participants/:participantId/video/:videoId` — participantId must be a real user UUID (a placeholder 500s the row query)
- **Prod verification pattern:** SPA returns 200 for any path — grep the *hashed page chunks* referenced by the main bundle for feature marker strings, then Playwright click-through for interactions
- **Plank `per_second`** exists on every engine-analyzed plank; re-analyze endpoint (`report = NULL` + re-queue) refreshes anything stale
- Plan-fixture bug caught by TDD: HandicapResultCard's spec fixture used the same value for index and differential → ambiguous `getByText`; fixture fixed, component kept to spec

## Open follow-ups (unchanged from the roadmap discussion)

- T17 reliability track: admin auth, CORS/rate limiting, branch protection + CI gate, uptime alerting
- 2026-07-10: delete dormant analysis pollers (PLANS.md Task 11); ~07-13 jobs-mode soak cleanup
- T9 emailed short-links → backend origin so they unfurl
- Dead code sweep: `FindProfile.tsx` dialog, `TopLifts.tsx`, `getFeaturedChallenges()`
- Plank v2 candidates: PB banner, challenge percentile, elbow-angle viz
- Real-device check of the camera `capture` flows
